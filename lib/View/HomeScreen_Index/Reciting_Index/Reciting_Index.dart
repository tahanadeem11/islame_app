import 'package:flutter/material.dart';
class Reciting_Index extends StatelessWidget {
  const Reciting_Index({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
